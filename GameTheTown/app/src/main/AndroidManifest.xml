<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gamethetown">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- android:theme="@style/Theme.AppCompat.Light.NoActionBar" -->
        <!-- android:theme="@style/AppTheme" -->
        <!-- Default Team -->
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Profile"
            android:label="Profile" />
        <activity android:name=".Reg" />
        <activity
            android:name=".CreateItenerary"
            android:label="Create Itinerary" />
        <activity
            android:name=".ListOfIteneraries"
            android:label="All Itineraries" />
        <activity android:name=".CurrentItenerary" />
        <activity android:name=".Filter" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCeNYa6YBbyFxVhNe_Ix7mWg-uGPZSHpUM" />

        <activity
            android:name=".fragment.Map_Current_Iten"
            android:label="@string/title_activity_map__current__iten" />
        <activity
            android:name=".ListOfCreatedItineraries"
            android:label="@string/title_activity_list_of_created_itineraries" />
        <activity android:name=".ListOfCompletedItineraries" />
        <activity
            android:name=".SearchAllItineraries"
            android:label="@string/title_activity_search_all_itineraries"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".gameControllers.Hotspot_Quiz_Creator" />
        <activity android:name=".gameControllers.HotspotQuiz"></activity>
    </application>

</manifest>